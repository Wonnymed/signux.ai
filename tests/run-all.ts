/**
 * Master test file — imports all test suites.
 * Run with: npm test
 *
 * Test count: 30
 * Expected time: < 30 seconds
 *
 * CATEGORIES:
 *   Memory (10):    facts, temporal, core memory, recall, behavioral
 *   Simulation (9): domain detection, agent selection, heatmap
 *   Chat (7):       tiers, chat with memory, kraken
 *   Auth (4):       tier access, userId resolution
 */

// This file exists to document the test suite.
// vitest discovers tests automatically from tests/**/*.test.ts
// Run: npm test
