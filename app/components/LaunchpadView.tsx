"use client";
export default function LaunchpadView({ lang }: { lang: string }) {
  return <div style={{ display: "flex", alignItems: "center", justifyContent: "center", minHeight: "calc(100vh - 60px)", color: "var(--text-tertiary)", fontSize: 14 }}>Launchpad — coming in next update</div>;
}
