import type { Metadata } from "next";
export const metadata: Metadata = { title: "Signux AI — Decision Operating System" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
